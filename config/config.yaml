artifacts_root: artifacts

#we are creating this function/directory so that in the future we don't that in the future we don't have to go and change the name of the file everywhere, it can be done by here itself and it will reflect in whole code

data_ingestion:
  root_dir: artifacts/data_ingestion
  source_URL: https://github.com/entbappy/Branching-tutorial/raw/master/summarizer-data.zip
  local_data_file: artifacts/data_ingestion/data.zip
  unzip_dir: artifacts/data_ingestion